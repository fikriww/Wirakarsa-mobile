class FirebaseConfig {
  /// The Firestore database ID to connect to.
  /// Mapped to the standard database created in your Firebase Console (e.g., 'database2', 'database22', or 'default').
  static const String databaseId = 'database2';
}
